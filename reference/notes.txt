Section 18 - HTTP REQUEST

Angular tidak langsung ambil data ke database.
Karena security reason.
Karena itu ambilnya lewat API (Rest/GraphQL);
Ada http request dan http response.

Anatomy HTTP Request.
    URL (API Endpoint)
    Http Verb (POST, GET, PUT) --> Untuk Restful
    Headers(Metadata) --> {"Content-Type":"application/json"} Ada defaultnya
    Body --> { title: "New POst" }

Backend Setup using Firebase
    firebase.google.com 
    Add Project
    Ng-Max
    Database
    Firebase adalah complete backend service
    Pilih Realtime Database
        Start in test mode
        https://ng-max.firebaseio.com/

Http Request adalah Observable, harus subscribe
Request only sent when you subscribe

Kasih pipe() sebelum subscribe 
map operator dari rxjs. 

id?: string <-- This is a string tapi optional. Tanda ? menandakan opsional

GET method adalah generic method 
    Bisa ditambahkan angle bracket berisi type which this response will actually return as a body once it's done.
    (Data you're getting back)

Kalau ada banyak component dan big codes, gunakan service supaya rapi

observe: 'response' akan mendapatkan balikan secara lengkap, defaultnya body saja.

observe: 'events' 